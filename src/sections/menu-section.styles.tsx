import styled, { css } from "styled-components";

// Base Styles
const baseMenu = css`
  display: flex;
`;

// Components
const Menu = styled.section`
  ${baseMenu}
`;

export const Styled = {
  Menu,
};
